export class BufferedWritable {
    constructor() {
        this.buffer = new Uint8Array(0);
    }
    write(chunk) {
        let buf = new Uint8Array(this.buffer.length + chunk.length);
        buf.set(this.buffer);
        buf.set(chunk, this.buffer.length);
        this.buffer = buf;
    }
    ;
}
//# sourceMappingURL=buffered-writable.js.map